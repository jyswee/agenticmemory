startCommand:
  type: stdio
  configSchema:
    type: object
    properties:
      apiKey:
        type: string
        description: Agentic Memory API key (amk_...). Optional for tool discovery; required to call a tool. Get one instantly with `agmry signup my-project`.
      baseUrl:
        type: string
        default: https://agenticmemory.ai
  commandFunction: |-
    (config) => ({
      command: "npx",
      args: ["-y", "agmry", "mcp-serve"],
      env: { AGMRY_API_KEY: config.apiKey, AGMRY_URL: config.baseUrl || "https://agenticmemory.ai" }
    })
